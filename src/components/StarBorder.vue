<template>
  <div
    :class="['relative inline-block overflow-hidden rounded-[20px]', className]"
    :style="{ padding: `${thickness}px 0`, ...style }"
  >
    <div
      class="absolute w-[300%] h-[50%] opacity-70 bottom-[-11px] right-[-250%] rounded-full animate-star-movement-bottom z-0"
      :style="{
        background: `radial-gradient(circle, ${color}, transparent 10%)`,
        animationDuration: speed,
      }"
    ></div>
    <div
      class="absolute w-[300%] h-[50%] opacity-70 top-[-10px] left-[-250%] rounded-full animate-star-movement-top z-0"
      :style="{
        background: `radial-gradient(circle, ${color}, transparent 10%)`,
        animationDuration: speed,
      }"
    ></div>
    <div class="relative z-1 bg-white border border-gray-800 text-white text-center text-[16px] py-[16px] px-[26px] rounded-[20px]">
      <slot />
    </div>
  </div>
</template>

<script setup>
defineProps({
  color: { type: String, default: 'white' },
  speed: { type: String, default: '6s' },
  thickness: { type: [Number, String], default: 1 },
  className: { type: String, default: '' },
  style: { type: Object, default: () => ({}) },
});
</script> 